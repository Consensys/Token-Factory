var FactoryPage = React.createClass({
  render: function() {
    return (
      <div>
        Enter the amount, the creator will receive: [form here]
      </div>
    );
  }
});

window.FactoryPage = FactoryPage;
